[project]
name = "taxi-pipeline"
version = "0.1.0"
description = "NYC Taxi Data Pipeline using dlt REST API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dlt[workspace]>=0.4.0",
    "requests>=2.31.0",
    "duckdb>=0.9.0"
]
